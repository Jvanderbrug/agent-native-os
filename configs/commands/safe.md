---
description: Enable Safe mode (Level 1 - Director's Cut)
---

Switch to Safe mode. Remove all auto-approve permissions from ~/.claude/settings.json so Claude asks permission for every action.

Preserve any other settings (env vars, MCP configs) -- only remove the permissions.allow array.

Confirm the switch and remind that /yolo can re-enable YOLO mode when ready.
